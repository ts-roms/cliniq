**/node_modules
**/dist
**/build
**/.next
# TS project-reference output + incremental caches. Gitignored, so a clean CI
# checkout never has them — but a dev's working tree does, and copying them in
# lets a stale .d.ts / tsbuildinfo from the host decide what `next build`'s
# typecheck sees. Keep the image building from source only.
**/out-tsc
**/*.tsbuildinfo
**/.expo
**/.turbo
**/storybook-static
**/coverage
**/test-output
**/.cache
**/.nx/cache
**/.nx/workspace-data

# Source-control + editor / agent noise. `.claude/worktrees/` in particular
# holds full checkouts of the repo (with their own build output) and was
# doubling the build context.
.git
.github
.claude
.vscode
.idea
.agents
.codex
.cursor
.gemini
.opencode
**/*.log

# Local dotenvs & secrets
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local

# Big media not needed for runtime
**/*.gif
**/*.mp4
docs/
