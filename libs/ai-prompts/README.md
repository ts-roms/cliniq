# ai-prompts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ai-prompts` to build the library.

## Running unit tests

Run `nx test ai-prompts` to execute the unit tests via [Jest](https://jestjs.io).
